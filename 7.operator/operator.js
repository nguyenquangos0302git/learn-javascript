// unary
const count = 1;
const negativeCount = -count; // -1
let idx = 1;
const incrementPrefix = ++idx; // 2
let idx1 = 1;
const incrementSuffix = idx1++; // 1
const quantity = +"2"; // number 2
const isValid = !true; // false

// ternary
let x = true ? "show" : "hiden";
